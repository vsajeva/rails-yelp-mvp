<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, placeholder: "Type here..."%>
  <%= f.input :rating,  placeholder: "Add a rating (1-5)" %>
  <%= f.submit "Add your review", class: "btn btn-primary" %>
<% end %>
